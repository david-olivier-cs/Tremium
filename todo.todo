General integration tasks : 
- [x] Allow host bluetooth interface to be used from inside docker container

# Tremium Hub tasks :
- [] Mount host folders used for storage by Hub
- [] Test the (Hub) bluetooth server once runnning inside a docker container
- [] Allow docker service to be used from within a docker container
- [] Setup cron job for the data collector